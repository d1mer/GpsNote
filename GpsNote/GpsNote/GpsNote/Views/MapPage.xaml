<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GpsNote.Views.MapPage"
             xmlns:viewModels="clr-namespace:GpsNote.ViewModels"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             xmlns:controls="clr-namespace:GpsNote.Controls"
             Title="Map">

    <!--<Grid HorizontalOptions="FillAndExpand"
          VerticalOptions="FillAndExpand">
        <maps:Map x:Name="map"
                  MyLocationEnabled="True"
                  VerticalOptions="FillAndExpand"
                  HasScrollEnabled="True"
                  HasZoomEnabled="True" 
                  MapType="{Binding MapType}">
            
            <maps:Map.Behaviors>
                <prism:EventToCommandBehavior EventName="MapClicked"
                                              Command="{Binding MapTapCommand}"
                                              EventArgsParameterPath="Point" />
                <prism:EventToCommandBehavior EventName="CameraIdled"
                                              Command="{Binding CameraIdLedCommand}"
                                              EventArgsParameterPath="Position" />
            </maps:Map.Behaviors>
        </maps:Map>
    </Grid>-->

    <Grid>
        <controls:CustomMap MapType="{Binding MapType}"
                            CurrentPosition="{Binding LastPosition}"
                            CurrentZoom="{Binding LastZoom}"
                            IsRequiredCameraChanging="{Binding IsRequiredCameraChanging}">
            
        </controls:CustomMap>
    </Grid>
    
</ContentPage>